<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Spese" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">?colorPrimary</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- tema senza aciton bar -->
    <style name="Theme.Spese.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Spese.Splashscreen">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="colorPrimary">?colorPrimary</item>
        <item name="android:forceDarkAllowed" tools:ignore="NewApi">false</item>
    </style>

    <style name="Theme.Spese.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Spese.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Colora toolbar -->
    <style name="ToolbarTheme" parent="Theme.Spese">
        <item name="android:textColorPrimary">?colorPrimary</item> <!-- colora il testo -->
        <item name="android:textColorSecondary">?colorPrimary</item> <!-- colora il sottotitolo -->
        <item name="colorButtonNormal">?colorPrimary</item><!-- colora le icone -->
        <item name="android:background">?colorOnPrimary</item> <!-- colore della toolbar -->
    </style>
</resources>